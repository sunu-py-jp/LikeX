import { officeXml } from "../ooxml";
export type { OfficeXmlNode as XmlNode } from "../ooxml";
export const { localName, children, child, attribute, textContent, parseXml, spreadsheetText } = officeXml;
