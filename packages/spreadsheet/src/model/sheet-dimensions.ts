/** Implicit persisted sizes when a sheet omits a row or column override. */
export const DEFAULT_ROW_HEIGHT = 28;
export const DEFAULT_COLUMN_WIDTH = 100;

/** Initial canvas size shared by new workbooks and newly added sheets. */
export const DEFAULT_SHEET_SIZE = Object.freeze({ rowCount: 300, columnCount: 26 });
