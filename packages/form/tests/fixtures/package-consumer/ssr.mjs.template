import assert from "node:assert/strict";
import { createElement } from "react"; import { renderToStaticMarkup } from "react-dom/server";
import LikeForm, { createForm } from "__LIBRARY_IMPORT__";
const markup = renderToStaticMarkup(createElement(LikeForm, { initialForm: createForm({ title: "LikeForm consumer" }), style: { height: 600 } }));
assert.match(markup, /data-likex-form/); assert.match(markup, /LikeForm consumer/);
console.log(JSON.stringify({ resolved: import.meta.resolve("__LIBRARY_RESOLVE__"), renderedBytes: markup.length }));
