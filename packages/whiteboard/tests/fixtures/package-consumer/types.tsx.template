import { createRef } from "react";
import LikeWhiteboard, { createWhiteboard, executeWhiteboardCommands, type WhiteboardHandle, type WhiteboardProps } from "__LIBRARY_IMPORT__";
const model = createWhiteboard({title:"LikeWhiteboard consumer"});
const props: WhiteboardProps = { initialWhiteboard: model, onSave: async current => current, features: {images: false}, primaryColor:"#2563eb" };
const ref = createRef<WhiteboardHandle>();
const view = <LikeWhiteboard ref={ref} {...props} />;
const current = executeWhiteboardCommands(model, {type:"whiteboard.update",title:"Changed"});
void ref.current?.getModel();void ref.current?.exportSvg();void ref.current?.select([]);void current;void view;
