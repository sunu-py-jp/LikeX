import assert from "node:assert/strict";
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import LikeDiagram, { LikeDiagram as Named, createDiagram } from "__LIBRARY_IMPORT__";
assert.equal(LikeDiagram,Named);assert.equal(typeof window,"undefined");
const model = createDiagram({title:"LikeDiagram consumer"});
const markup = renderToStaticMarkup(createElement(LikeDiagram,{initialDiagram:model,style:{height:360}}));
assert.match(markup,/data-likex-diagram/);assert.match(markup,/LikeDiagram consumer/);assert.match(markup,/height:360px/);
console.log(JSON.stringify({resolved:import.meta.resolve("__LIBRARY_RESOLVE__"),renderedBytes:markup.length}));
