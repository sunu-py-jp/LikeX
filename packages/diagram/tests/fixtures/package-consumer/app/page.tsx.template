import LikeDiagram from "__LIBRARY_IMPORT__";
import { createDiagram, executeDiagramCommands } from "__LIBRARY_MODEL__";
import Client from "./client";
export default function Page() {
  const model = executeDiagramCommands(createDiagram(), { type: "diagram.update", title: "LikeDiagram consumer" });
  return <main><LikeDiagram initialDiagram={model} style={{ height: 360 }} /><Client /></main>;
}
