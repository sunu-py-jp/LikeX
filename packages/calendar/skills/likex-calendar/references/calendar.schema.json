{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Calendar",
  "$comment": "Generated by scripts/build-skill-schemas.mjs (generator v1; TypeScript 5.9.3; @likex/calendar 0.1.0) from packages/calendar/src/model/types.ts#Calendar. Do not edit. Structural JSON constraints only; runtime parsers and command APIs are authoritative for limits, references, and semantic validity. Undefined alternatives are omitted because JSON cannot represent undefined.",
  "$ref": "#/$defs/Calendar",
  "$defs": {
    "Calendar": {
      "type": "object",
      "properties": {
        "format": {
          "const": "likex.calendar"
        },
        "version": {
          "const": 1
        },
        "id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "timeZone": {
          "type": "string",
          "description": "IANA display timezone. Stored timed events always retain their instant."
        },
        "events": {
          "$ref": "#/$defs/Calendar_events"
        }
      },
      "required": [
        "format",
        "version",
        "id",
        "title",
        "timeZone",
        "events"
      ],
      "additionalProperties": false
    },
    "Calendar_events": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/Calendar_events_item"
      }
    },
    "Calendar_events_item": {
      "type": "object",
      "properties": {
        "id": {
          "type": "string"
        },
        "title": {
          "type": "string"
        },
        "description": {
          "type": "string"
        },
        "location": {
          "type": "string"
        },
        "color": {
          "type": "string"
        },
        "allDay": {
          "type": "boolean"
        },
        "start": {
          "type": "string"
        },
        "end": {
          "type": "string",
          "description": "Exclusive end, including for all-day events."
        }
      },
      "required": [
        "id",
        "title",
        "allDay",
        "start",
        "end"
      ],
      "additionalProperties": false
    }
  }
}
