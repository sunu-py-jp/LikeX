import LikeCalendar from "__LIBRARY_IMPORT__";
import { createCalendar, executeCalendarCommands } from "__LIBRARY_MODEL__";
import ClientCalendar from "./client";
export default function Page() {
  const calendar = createCalendar({ title: "LikeCalendar consumer" });
  const result = executeCalendarCommands(calendar, { type: "event.create", event: { title: "Server calendar event", allDay: true, start: "2026-09-22", end: "2026-09-23" } });
  return <main><LikeCalendar initialCalendar={result.calendar} initialDate="2026-09-22" style={{ height: 600 }} /><ClientCalendar /></main>;
}
