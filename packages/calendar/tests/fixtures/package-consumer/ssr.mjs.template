import assert from "node:assert/strict";
import { createElement } from "react";
import { renderToStaticMarkup } from "react-dom/server";
import LikeCalendar, { LikeCalendar as NamedLikeCalendar, createCalendar } from "__LIBRARY_IMPORT__";
assert.equal(LikeCalendar, NamedLikeCalendar);
assert.equal(typeof window, "undefined");
const calendar = createCalendar({ title: "LikeCalendar consumer", events: [{ id: "event", title: "Calendar consumer event", allDay: true, start: "2026-09-22", end: "2026-09-23" }] });
const markup = renderToStaticMarkup(createElement(LikeCalendar, { initialCalendar: calendar, initialDate: "2026-09-22", style: { height: 600 } }));
assert.match(markup, /data-likex-calendar/); assert.match(markup, /Calendar consumer event/); assert.match(markup, /height:600px/);
console.log(JSON.stringify({ resolved: import.meta.resolve("__LIBRARY_RESOLVE__"), renderedBytes: markup.length }));
